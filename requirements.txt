# Core dependencies
numpy>=1.26.4
opencv-python>=4.10.0.84

# Install YOLOv10 from the local folder (clone into ./yolov10 first)
-e ./yolov10

# OCR - EasyOCR for Bangla + English license plate recognition
easyocr>=1.7.0
torch>=2.0.0

# Optional: Tesseract fallback (requires Tesseract installed on system)
pytesseract>=0.3.10
Pillow>=10.0.0

# Utilities
huggingface-hub